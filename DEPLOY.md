# 🚀 Инструкция по развертыванию (Deploy) на Render.com

Эта инструкция поможет вам бесплатно запустить ваш Telegram-магазин в интернете.

## ✅ Шаг 1: Подготовка
Убедитесь, что у вас есть:
1.  Аккаунт на [GitHub](https://github.com/).
2.  Аккаунт на [Render.com](https://dashboard.render.com/) (войдите через GitHub).
3.  Установленный [Git](https://git-scm.com/).

## 📤 Шаг 2: Загрузка кода на GitHub
Нам нужно отправить код вашего проекта в репозиторий GitHub.

1.  Создайте новый **пустой** репозиторий на GitHub (назовите его, например, `fashion-store-tg`).
2.  Откройте терминал в папке вашего проекта (`fashion-store-tg`) и выполните команды:

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Сохранение изменений
git commit -m "Initial commit"

# Связывание с вашим репозиторием (ЗАМЕНИТЕ ССЫЛКУ НА ВАШУ!)
git remote add origin https://github.com/ВАШ_НИК/fashion-store-tg.git

# Отправка кода
git push -u origin master
```

## 🚀 Шаг 3: Настройка Render
Мы будем использовать файл `render.yaml`, который я уже создал. Он сделает почти всё за вас.

1.  Зайдите в [Render Dashboard](https://dashboard.render.com/).
2.  Нажмите кнопку **New +** (вверху справа) и выберите **Blueprint**.
3.  Подключите ваш GitHub аккаунт (если спросит) и выберите репозиторий `fashion-store-tg`.
4.  Напишите название для сервиса (Service Group Name): `FashionStore`.
5.  Нажмите **Apply**.

## 🔑 Шаг 4: Настройка переменных (Secrets)
Render увидит файл конфигурации и попросит вас ввести секретные данные. Заполните их:

| Переменная | Значение / Где взять |
|------------|----------------------|
| `SUPABASE_URL` | Ссылка из настроек вашего Supabase проекта (Project Settings -> API) |
| `SUPABASE_KEY` | Ключ `service_role` (или `anon`) из Supabase (Project Settings -> API) |
| `TELEGRAM_BOT_TOKEN` | Токен вашего бота от @BotFather |
| `KASPI_PHONE` | Номер телефона для переводов (например, `+77771234567`) |
| `KASPI_QR_URL` | Ссылка на картинку вашего QR (можно загрузить на любой хостинг картинок) |
| `ADMIN_TELEGRAM_IDS` | Ваш telegram ID (можно узнать у бота @userinfobot). Если несколько, через запятую. |
| `FRONTEND_URL` | **Пока оставьте пустым**, мы заполним это после первого запуска, или введите `https://fashion-store-frontend.onrender.com` (имя, которое даст Render) |

⚠️ **Важно:** `VITE_API_URL` вводить не нужно! Render сам свяжет фронтенд с бэкендом.

Нажмите **Apply Changes**.

## ⏳ Шаг 5: Ожидание сборки
Render начнет собирать проект.
1.  Сначала запустится **Backend**.
2.  После успеха бэкенда начнется сборка **Frontend**.

Это может занять 5-10 минут. Следите за логами в панели Render.

## 🤖 Шаг 6: Подключение к Telegram
Когда деплой завершится (зеленые галочки ✅):

1.  В панели Render найдите сервис **frontend** (static site).
2.  Скопируйте его URL (например, `https://fashion-store-frontend-xxxx.onrender.com`).
3.  Откройте **@BotFather** в Telegram.
4.  Напишите `/mybots` -> выберите бота -> **Bot Settings** -> **Menu Button** -> **Configure Menu Button**.
5.  Отправьте ссылку на ваш сайт.
6.  (Опционально) Также добавьте ссылку в описание бота.

## 🎉 Готово!
Теперь ваш магазин доступен в Telegram для всех пользователей!

---

### ❓ Проблемы?
Если сайт не загружается или данные не приходят:
*   Проверьте логи сервиса **backend** в Render (вкладка Logs).
*   Убедитесь, что в переменных окружения нет лишних пробелов.
*   Убедитесь, что Supabase не блокирует соединения (проверьте настройки Network Restrictions в Supabase).
